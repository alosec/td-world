---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>td — task tracker for the terminal</title>
    <meta name="description" content="SQLite task tracker. Single bash script. Works offline." />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      
      :root {
        --bg: #0d1117;
        --fg: #e6edf3;
        --muted: #7d8590;
        --accent: #58a6ff;
        --surface: #161b22;
        --border: #30363d;
        --green: #3fb950;
        --mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      }
      
      html {
        font-family: var(--mono);
        background: var(--bg);
        color: var(--fg);
        font-size: 15px;
        line-height: 1.6;
      }
      
      body {
        min-height: 100vh;
        max-width: 720px;
        margin: 0 auto;
        padding: 4rem 2rem;
      }
      
      header {
        margin-bottom: 3rem;
      }
      
      h1 {
        font-size: 2rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }
      
      h1 .cursor {
        display: inline-block;
        width: 0.6em;
        height: 1.2em;
        background: var(--accent);
        animation: blink 1s step-end infinite;
      }
      
      @keyframes blink {
        50% { opacity: 0; }
      }
      
      .tagline {
        color: var(--muted);
        font-size: 1rem;
      }
      
      .install-box {
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 6px;
        padding: 1rem 1.25rem;
        margin-bottom: 3rem;
        position: relative;
        overflow-x: auto;
      }
      
      .install-box code {
        color: var(--green);
        word-break: break-all;
        font-size: 0.85rem;
      }
      
      .install-box .prompt {
        color: var(--muted);
        user-select: none;
      }
      
      .copy-btn {
        position: absolute;
        right: 0.75rem;
        top: 0.75rem;
        background: var(--border);
        border: none;
        color: var(--muted);
        padding: 0.4rem 0.6rem;
        border-radius: 4px;
        cursor: pointer;
        font-size: 0.75rem;
        font-family: var(--mono);
      }
      
      .copy-btn:hover {
        background: var(--muted);
        color: var(--bg);
      }
      
      section {
        margin-bottom: 3rem;
      }
      
      h2 {
        font-size: 0.8rem;
        font-weight: 500;
        color: var(--muted);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px solid var(--border);
      }
      
      .features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
      }
      
      .feature {
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 6px;
        padding: 1rem;
      }
      
      .feature-title {
        color: var(--accent);
        margin-bottom: 0.25rem;
      }
      
      .feature-desc {
        color: var(--muted);
        font-size: 0.85rem;
      }
      
      .commands {
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 6px;
        overflow: hidden;
      }
      
      .cmd-row {
        display: flex;
        padding: 0.6rem 1rem;
        border-bottom: 1px solid var(--border);
      }
      
      .cmd-row:last-child {
        border-bottom: none;
      }
      
      .cmd {
        color: var(--green);
        min-width: 280px;
      }
      
      .cmd-desc {
        color: var(--muted);
        font-size: 0.9rem;
      }
      
      .stats {
        display: flex;
        gap: 2rem;
        flex-wrap: wrap;
      }
      
      .stat {
        text-align: center;
      }
      
      .stat-value {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--fg);
      }
      
      .stat-label {
        font-size: 0.75rem;
        color: var(--muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }
      
      footer {
        display: flex;
        gap: 1.5rem;
        padding-top: 2rem;
        border-top: 1px solid var(--border);
      }
      
      a {
        color: var(--accent);
        text-decoration: none;
        font-size: 0.9rem;
      }
      
      a:hover {
        text-decoration: underline;
      }
      
      .tv-note {
        color: var(--muted);
        font-size: 0.85rem;
        margin-top: 1rem;
      }
      
      .tv-note code {
        color: var(--accent);
      }
      
      @media (max-width: 600px) {
        html { font-size: 14px; }
        body { padding: 2rem 1rem; }
        .install-box { padding: 0.75rem 1rem; padding-right: 4rem; }
        .install-box code { font-size: 0.75rem; }
        .copy-btn { top: 0.5rem; right: 0.5rem; }
        .features { grid-template-columns: 1fr 1fr; gap: 0.75rem; }
        .feature { padding: 0.75rem; }
        .cmd { min-width: auto; margin-bottom: 0.25rem; font-size: 0.8rem; }
        .cmd-row { flex-direction: column; padding: 0.5rem 0.75rem; }
        .cmd-desc { font-size: 0.75rem; }
        .stats { gap: 1.5rem; justify-content: space-between; }
        .stat-value { font-size: 1.25rem; }
        footer { gap: 1rem; }
      }
      
      @media (max-width: 400px) {
        .features { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>td<span class="cursor"></span></h1>
      <p class="tagline">SQLite task tracker for the terminal</p>
    </header>
    
    <div class="install-box">
      <span class="prompt">$ </span><code>curl -fsSL https://raw.githubusercontent.com/alosec/td-world/main/install.sh | bash</code>
      <button class="copy-btn" onclick="copy()">copy</button>
    </div>
    
    <section>
      <h2>Why</h2>
      <div class="features">
        <div class="feature">
          <div class="feature-title">Single file</div>
          <div class="feature-desc">One bash script. Copy anywhere.</div>
        </div>
        <div class="feature">
          <div class="feature-title">SQLite</div>
          <div class="feature-desc">Real database. Query with SQL.</div>
        </div>
        <div class="feature">
          <div class="feature-title">Offline</div>
          <div class="feature-desc">No accounts. No sync. Just files.</div>
        </div>
        <div class="feature">
          <div class="feature-title">Fast</div>
          <div class="feature-desc">~15ms. No startup cost.</div>
        </div>
      </div>
    </section>
    
    <section>
      <h2>Commands</h2>
      <div class="commands">
        <div class="cmd-row">
          <span class="cmd">td create "fix bug" -p 1</span>
          <span class="cmd-desc">create with priority</span>
        </div>
        <div class="cmd-row">
          <span class="cmd">td list</span>
          <span class="cmd-desc">show all tasks</span>
        </div>
        <div class="cmd-row">
          <span class="cmd">td move abc-123 todo</span>
          <span class="cmd-desc">change status</span>
        </div>
        <div class="cmd-row">
          <span class="cmd">td ready</span>
          <span class="cmd-desc">unblocked tasks</span>
        </div>
        <div class="cmd-row">
          <span class="cmd">td close abc-123</span>
          <span class="cmd-desc">mark done</span>
        </div>
        <div class="cmd-row">
          <span class="cmd">td dep add abc-123 xyz-456</span>
          <span class="cmd-desc">add blocker</span>
        </div>
      </div>
      <p class="tv-note">Want a TUI? <code>tv</code> gives you fzf-powered table and kanban views.</p>
    </section>
    
    <section>
      <h2>Stats</h2>
      <div class="stats">
        <div class="stat">
          <div class="stat-value">884</div>
          <div class="stat-label">lines</div>
        </div>
        <div class="stat">
          <div class="stat-value">~15ms</div>
          <div class="stat-label">runtime</div>
        </div>
        <div class="stat">
          <div class="stat-value">0</div>
          <div class="stat-label">deps</div>
        </div>
        <div class="stat">
          <div class="stat-value">1</div>
          <div class="stat-label">file</div>
        </div>
      </div>
    </section>
    
    <footer>
      <a href="https://github.com/alosec/td-world">GitHub</a>
      <a href="https://github.com/alosec/td-world#usage">Docs</a>
      <a href="https://github.com/alosec/td-world/blob/main/bin/td">Source</a>
    </footer>
    
    <script>
      function copy() {
        navigator.clipboard.writeText('curl -fsSL https://raw.githubusercontent.com/alosec/td-world/main/install.sh | bash');
        const btn = document.querySelector('.copy-btn');
        btn.textContent = 'copied!';
        setTimeout(() => btn.textContent = 'copy', 1500);
      }
    </script>
  </body>
</html>
